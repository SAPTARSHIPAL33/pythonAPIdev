**py -m venv myvenv**------- first venv is to declare the virtual env. myvenv is the name of the virtual env(can be anything)
**myvenv/Scripts/activate**----------- to activate venv
we should maintain the order of the path parameters. it maybe possible in some cases that what we want to excute wont be executed the other might get executed. video timeframe-1:50:00(approx)
sqlalchemy is one of the most popular python ORM that helps to interact codebase with database without any sql command. Only python codes
SQLalchemy cant talk to database we need a database driver--- here as the database is postgresql we are using psycopg2 driver to make it interact database